# fluentd

this example runs:

- a fluentd log collection service
- a node service that logs
- a python service that logs

## usage

- `cd <this-dir>` # recommended
- `docker-compose up`

then peek around
